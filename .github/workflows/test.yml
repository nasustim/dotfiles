name: Test

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test:
    timeout-minutes: 30
    strategy:
      matrix:
        os: 
          - macos-latest
          - ubuntu-latest
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6.0.2
      - uses: jdx/mise-action@v3.6.1
      - run: make
      - run: make clean
