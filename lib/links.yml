# Dotfiles Link Configuration
# YAML format for defining symbolic links

links:
  # vim configuration
  - source: confs/.vimrc
    destination: .vimrc

  # tmux configuration  
  - source: confs/.config/tmux/.tmux.conf
    destination: .config/tmux/.tmux.conf

  # git configuration
  - source: confs/.config/git/ignore
    destination: .config/git/ignore

  - source: confs/.config/git/config
    destination: .config/git/config

  - source: confs/.config/git/config_alt
    destination: .config/git/config_alt

  # zsh configuration
  - source: confs/.zsh
    destination: .zsh

  - source: confs/.zshenv
    destination: .zshenv

  - source: confs/.zshrc
    destination: .zshrc

  # claude configuration
  - source: confs/.claude/CLAUDE.md
    destination: .claude/CLAUDE.md

  - source: confs/.claude/settings.json
    destination: .claude/settings.json

  # mise
  - source: confs/.config/mise/config.toml
    destination: .config/mise/config.toml
